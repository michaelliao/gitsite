/** @type {import('tailwindcss').Config} */
export default {
    darkMode: 'class',
    content: ['./**/*.html'],
    theme: {
        extend: {},
    },
    plugins: ['forms', 'typography', 'aspect-ratio'],
}
